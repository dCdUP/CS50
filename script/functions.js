function rechnen() {
    let eingabe = document.getElementById("zahl").value;
    value = parseInt(eingabe) + 1;
    console.log(eingabe);
}

const update = () => {

}

const button = document.getElementById("Knopf")

button.addEventListener("input", calc)